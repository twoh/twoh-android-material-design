<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".application.TWOHsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CarouselActivity"
            android:exported="false"
            android:label="@string/title_activity_carousel"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FloatingActionButtonActivity" />
        <activity android:name=".TextInputActivity" />
        <activity
            android:name=".NavigationViewActivity" />
        <activity
            android:name=".AppContainerTabActivity" />
        <activity
            android:name=".TabLayoutActivity" />
        <activity
            android:name=".CollapsingToolbarActivity" />
        <activity
            android:name=".RefreshViewActivity" />
        <activity
            android:name=".RecyclerViewActivity" />
        <activity
            android:name=".PaletteActivity" />
        <activity
            android:name=".SwitchCompatActivity"/>
        <activity
            android:name=".RippleEffectActivity"/>
        <activity
            android:name=".RatingBarActivity"/>
        <activity
            android:name=".BottomNavigationViewActivity" />
    </application>

</manifest>